// Build system uses defaults with `environment.ts`, run `ng build --env=prod` - then `environment.prod.ts` will be used.
// List of which env maps to which file can be found in `.angular-cli.json`.

export const environment = {
  production: false
};
